language: android
android:
  components:
    - tools
    - platform-tools
    - build-tools-23.0.2
    - android-23
    - extra-android-m2repository

jdk: oraclejdk8

notifications:
  email: false

script:
    - ./gradlew build connectedCheck

env:
    - GRADLE_OPTS="-Xmx512m -XX:MaxPermSize=512m"
